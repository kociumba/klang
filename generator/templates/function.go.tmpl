{{- define "function" -}}
  {{- if .ReturnType}}{{template "type" .ReturnType}}{{else}}void{{end}} {{.Name}}(
    {{- range $index, $param := .Params -}}
      {{if $index}}, {{end}}{{template "type" $param.Type}} {{$param.Name}}
    {{- end -}}
  ) {
    {{template "block" .Body}}
  }
{{- end -}}